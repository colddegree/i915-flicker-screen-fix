#Flickering screen fix for Intel i915
using modprobe

##Installation
Clone repository and `cd` to it. Then execute following command:
```
$ sudo cp -rn root/* /
```

##Configuration
/etc/intelpwm.conf

